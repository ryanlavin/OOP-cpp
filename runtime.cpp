#include <iostream>

int main(){
    int n; // user inputs n
    for(int i=0;i<n;i++){ // takes n steps right?
        // do something simple
            for(int j = 0;j < n;j++){

            }
    }
    // o(n)
    // o(n^2)
    // o(log n )
    // o(n!)
    // o(log log(n)) this is for instancethe runtime of interpoloation seach give this a look
}
